This is my first Git file!

now i am adding new lines to test my second commit
FINDING RUNNER UP IN GIVEN INPUTS              
if __name__ == '__main__':                 

    n = int(input())
    arr = list(map(int, input().split()))
    scores = list(set(arr))
    scores.sort(reverse=True)
    print(scores[1])
EMPLOYEE SALARY CALCULATOR
class Employee:
    def __init__(self,name,salary):
        self.name = name
        self.salary = salary
    def bonus(self,bonus):
        self.salary+=bonus
        print(f'new salary is {self.salary}')
employee = Employee(name,salary)
name = input('enter ur name')
salary = int(input('enter ur salary'))
bonus = float(input('enter ur bonus'))
employee.bonus(bonus)


TO FIND RADIUS AND CIRCUM USING FUNCTION
import math
class circle:
    def __init__(self,radius):
        self.radius=radius
    def aish(self):
        circum = 2*3.14*radius
        area = 3.14*radius*radius
        print('circum',circum)
        print('area',area)
radius = int(input('enter radius'))
Circle = circle(radius)
Circle.aish()



trying to upload again in github with changes made here

